version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: '3.12'


sphinx:
  configuration: docs/conf.py
  builder: html

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
